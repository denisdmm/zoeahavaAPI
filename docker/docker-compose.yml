version: '3.1'

services:
  db:
    build: .
    container_name: postgres-anava
    restart: always
    ports:
      -"5432:5432"
    volumes:
      -.docker/dbdata: /var/lib/postgresql/data
    environment:
      - POSTGRE_PASSWORD=docker
      - POSTGRE_DB=ZOEAHAVA